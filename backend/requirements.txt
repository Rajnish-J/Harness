annotated-doc==0.0.5
annotated-types==0.8.0
anthropic==1.0.0
anyio==4.14.2
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.4.2
distro==1.9.0
docstring_parser==0.18.0
fastapi==0.141.1
h11==0.16.0
httpcore==1.0.9
httpcore2==2.12.0
httptools==0.8.0
httpx==0.28.1
httpx2==2.12.0
idna==3.19
iniconfig==2.3.0
jiter==0.16.0
jsonpatch==1.33
jsonpointer==3.1.1
langchain-core==1.6.0
langchain-protocol==0.0.18
# langchain-core arrives as a hard dependency of langgraph (langchain-core<2,>=1.4.7).
# Nothing in app/ imports it: LangGraph is used purely as a DAG execution engine,
# and the agent loop in app/agent/loop.py remains hand-written.
langgraph==1.2.11
langgraph-checkpoint==4.2.0
langgraph-checkpoint-postgres==3.1.2
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.3
langsmith==0.11.1
openai==3.3.1
groq==1.7.0
orjson==3.12.0
ormsgpack==1.12.2
packaging==26.3
pluggy==1.6.0
psycopg==3.3.4
psycopg-binary==3.3.4
psycopg-pool==3.3.1
pydantic==2.13.4
pydantic-settings==2.15.0
pydantic_core==2.46.4
Pygments==2.21.0
pytest==9.1.1
pytest-asyncio==1.4.0
python-dotenv==1.2.3
PyYAML==6.0.3
requests==2.34.2
requests-toolbelt==1.0.0
sniffio==1.3.1
starlette==1.6.0
tenacity==9.1.4
truststore==0.10.4
typing-inspection==0.4.4
typing_extensions==4.16.0
urllib3==2.7.0
uuid_utils==0.17.0
uvicorn==0.52.4
# No Windows wheels exist and the sdist refuses to build there; nothing in app/
# imports it, and uvicorn falls back to the asyncio loop when it is absent.
uvloop==0.22.1; sys_platform != "win32"
watchfiles==1.2.0
websockets==16.1.1
xxhash==4.0.1
zstandard==0.25.0

# --- MCP client -------------------------------------------------------------
# Connects the servers configured in the mcp_servers table and exposes their
# tools to the agent. Pins below are what `pip install mcp` resolved to, taken
# from pip freeze rather than guessed; `pip check` is clean against the existing
# starlette/pydantic/httpx pins.
mcp==2.1.1
mcp-types==2.1.1
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
referencing==0.37.0
rpds-py==2026.6.3
attrs==26.1.0
sse-starlette==3.4.8
python-multipart==0.0.32
PyJWT==2.13.0
cryptography==50.0.1
cffi==2.1.1
pycparser==3.0
opentelemetry-api==1.44.0
# stdio transport reads process handles on Windows; no-op elsewhere.
pywin32==312; sys_platform == "win32"

# --- Docker ------------------------------------------------------------------
# Runs each project's commands inside its own container, so a repo's toolchain
# and dependencies never have to exist on the host. The SDK is synchronous, so
# every call is wrapped in asyncio.to_thread (see app/projects/containers.py).
docker==7.2.0
