# {{project_name}}

A Next.js app using the App Router, TypeScript and Tailwind CSS v4.

## Run it

```
npm install
npm run dev
```

Then open http://localhost:3000. Editing `app/page.tsx` hot-reloads.

## Note for agents

This is Next.js 16. Its App Router conventions differ from most training data —
check `node_modules/next/dist/docs/` before writing routing or config code.
