"""Tools about the harness itself rather than about the user's code.

Nothing here belongs in ALL_TOOLS -- see request_tools for why -- so this
package is deliberately not imported by registry.py.
"""
